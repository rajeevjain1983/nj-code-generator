import CustomErrorBoundary from '@examples/components/CustomErrorBoundary';
import { CustomFetchError } from "@verizon-nextgen/shared-ui";
import styles from './index.module.css';

/* eslint-disable-next-line */
export interface {{pascalCase name}}Props {}

export function {{pascalCase name}}(props: {{pascalCase name}}Props) {

   const errorRes = {
    ok: false,
  };

  const error = new CustomFetchError(errorRes as Response, {});
  const fallbackUI = () => {
    return (
      <div>
        <h2>Related Products.</h2>
      </div>
    );
  };

   return (
    <CustomErrorBoundary error={error} fallback={fallbackUI}>
       <div className={styles['container']}>
        <h1>Welcome to {{pascalCase name}}!</h1>
      </div>
    </CustomErrorBoundary>
  );
}

export default {{pascalCase name}};
